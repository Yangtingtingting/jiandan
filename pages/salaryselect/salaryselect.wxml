<!--pages/salaryselect/salaryselect.wxml-->
<view class="wrap">
    <view class="salarybox">
        <view class="salaryitem" wx:for="{{salaryintervallist}}" wx:key="index" data-id="{{item.id}}" bindtap="salarytap">
            {{item.text}}
        </view>
    </view>
</view>
